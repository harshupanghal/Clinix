<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <base href="/" />
    <link rel="stylesheet" href="@Assets["lib/bootstrap/dist/css/bootstrap.min.css"]" />
    <link rel="stylesheet" href="@Assets["app.css"]" />
    <link rel="stylesheet" href="@Assets["Clinix.Web.styles.css"]" />
    <ImportMap />
    <link rel="icon" type="image/png" href="favicon.png" />
    <HeadOutlet />
</head>

<body>
    <Routes />
    <script src="_framework/blazor.web.js"></script>
    @* <script>
        // Expose a small helper to redirect from JavaScript; used as the final fallback.
        window.blazorHelpers = window.blazorHelpers || {};
        window.blazorHelpers.forceRedirect = function (url, replace) {
          try {
            if (!url) return;
            if (replace) {
              // replace avoids extra history entry
              window.location.replace(url);
            } else {
              window.location.assign(url);
            }
          } catch (e) {
            console && console.error && console.error("forceRedirect failed", e);
          }
        }
    </script> *@

</body>

</html>
